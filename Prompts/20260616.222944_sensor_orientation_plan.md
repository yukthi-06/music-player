# User Prompt
when the mobile phone is tilted upside down, the app has to also turn aound and become vertical again, like google maps does
Copy implementation from 'X:\Projects_X\0_Active\8_Android_APK\01_GPS\GPS_Location_Logger_APK_GITHUB\

# Sensor Orientation Plan

This plan details changes to set screen orientation to `fullSensor` for all activities in the Music Player app, allowing the app to rotate in all directions (including upside down) based on device sensors.

## User Review Required

> [!NOTE]
> We will add the `android:screenOrientation="fullSensor"` attribute to all activities in `AndroidManifest.xml` to allow auto-rotation in all directions, including upside-down (reverse portrait) rotation, matching the behavior in GPS Location Logger.

## Proposed Changes

### [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Music_Player_APK_GITHUB/app/src/main/AndroidManifest.xml)

#### [MODIFY] [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Music_Player_APK_GITHUB/app/src/main/AndroidManifest.xml)
- Add `android:screenOrientation="fullSensor"` to:
  - `MainActivity`
  - `HelpActivity`
  - `AboutActivity`
  - `SettingsActivity`
  - `GeneralSettingsActivity`

## Verification Plan

### Manual Verification
- Statically check `AndroidManifest.xml` structure to ensure the attribute is added correctly.
