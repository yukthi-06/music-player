# User Prompt
upgrade java to 17

# Upgrade Java to 17 Plan

This plan details the changes to upgrade the Java version compatibility in the Music Player APK project to Java 17.

## User Review Required

> [!NOTE]
> We will upgrade the Java source and target compatibility options in the app's `build.gradle` file to `JavaVersion.VERSION_17`. This ensures the compiler builds the project targeting Java 17 features and bytecode compatibility.

## Proposed Changes

### [app/build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Music_Player_APK_GITHUB/app/build.gradle)

#### [MODIFY] [build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Music_Player_APK_GITHUB/app/build.gradle)
- Update `sourceCompatibility` to `JavaVersion.VERSION_17`.
- Update `targetCompatibility` to `JavaVersion.VERSION_17`.

## Verification Plan

### Manual Verification
- Verify that the gradle file parses successfully with Java 17 target configurations.
